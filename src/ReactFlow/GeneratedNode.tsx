/// Node type generated in flow
import { Handle, Position } from "reactflow"

const GeneratedNode = ({ title }: { title: string}) => {
  return (
    <>
        <div>
            <label htmlFor="text">{title}</label>
        </div>
    </>
  )
}

export default GeneratedNode